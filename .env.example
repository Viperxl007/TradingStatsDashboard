# Prevent automatic browser opening when running npm start
BROWSER=none

# Frontend port
PORT=3001

# Disable source map generation for faster builds (optional)
GENERATE_SOURCEMAP=false

# Backend Market Data Configuration
# Choose your market data source: 'yfinance' (default, free) or 'alphavantage' (requires API key)
MARKET_DATA_SOURCE=yfinance

# AlphaVantage API Key (only required if using MARKET_DATA_SOURCE=alphavantage)
# Get your free API key from: https://www.alphavantage.co/support/#api-key
# ALPHAVANTAGE_API_KEY=your-api-key-here